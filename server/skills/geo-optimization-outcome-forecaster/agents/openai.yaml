interface:
  display_name: "GEO 优化效果评估"
  short_description: "生成完整执行后不低于 60 分的一个月可验证目标区间"
  brand_color: "#3D1560"
  default_prompt: "Use $geo-optimization-outcome-forecaster to map all thirteen GEO indicators to auditable evidence-derived one-month intervals; the server applies the disclosed full-execution planning target of at least 60/100 and, when headroom permits, at least 10 points above the conservative current score, capped below 100."
